import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.AnnotationConfiguration;

import java.util.List;
 
 
public class Main {
 
    public static void main(String[] args) {

        /*
        // Read
        System.out.println("******* READ *******");
        List Persons = list();
        System.out.println("Total Persons: " + Persons.size());
         

        // Write
        System.out.println("******* WRITE *******");
        Person person = new Person("Jack", "Bauer");
        person.setAddress(new Address("123 Fake Street", "Springfield", "SP1F 123"));
        person.getAddress().setPerson(person);

        System.out.println("Saving person " + person);
        person = save(person);
        System.out.println("Reading person with id " + person.getId());
        person = read(person.getId());
        System.out.println("Got person: " + person);
        System.out.printf("%d %s %s \n", person.getId(), person.getFirstName(), person.getLastName());

        // Update
        System.out.println("******* UPDATE *******");
        Person empl2 = read(1); // read Person with id 1
        System.out.println("Name Before Update:" + empl2.getFirstName());
        empl2.setFirstName("James");
        update(empl2);  // save the updated Person details
         
        empl2 = read(1); // read again Person with id 1
        System.out.println("Name After Update:" + empl2.getFirstName());


        // Delete
        System.out.println("******* DELETE *******");
        delete(person);
        Person empl3 = read(person.getId());
        System.out.println("Object:" + empl3);
        */
    }
     


}